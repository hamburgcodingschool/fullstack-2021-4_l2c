// Split
// Transform a String into an Array

let greeting = "Hello everyone, how are you doing?";

// split parameter is the `seperator`, play with it and watch the results
let greetingWords = greeting.split("");


for (let i = 0; i < greetingWords.length; i++) {
    console.log(greetingWords[i]);
}